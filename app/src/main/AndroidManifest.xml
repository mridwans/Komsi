<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.komsi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TambahPenggunaActivityTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/TambahPenggunaActivityTheme" />
        <activity
            android:name=".DataUserActivity"
            android:label="@string/title_activity_data_user" />
        android:theme="@style/AppTheme" />
        <activity android:name=".TambahPenggunaActivity" />
        <activity android:name=".AddEventActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".AddSubfolderActivity" />
        <activity android:name=".ListItemActivity" />
        <activity android:name=".LogActivity" />
        <activity android:name=".AddSubSubFolderActivity" />
        <activity android:name=".MyDocumentActivity" />
        <activity android:name=".EventActivity"></activity>
    </application>

</manifest>